no_operation:
  doc: Does nothing


debug_print_eax:
  doc: Dumps the EAX register
  in:
    eax: Register to dump

terminate_process:
  doc: Makes the CPU sleep
  in:
    eax: Status code


read:
  in:
    esi: File descriptor
    eax: Bytes to read
    edi: Buffer
  out:
    edi: Filled buffer

write:
  in:
    edi: File descriptor
    eax: Bytes to read
    esi: Buffer
  out: